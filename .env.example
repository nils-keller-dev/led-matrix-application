# LED Matrix Application Configuration
# Copy this file to .env and update with your actual values

# Set to True when running locally to use the RGBMatrixEmulator
# Set to False when running on actual Raspberry Pi hardware
USE_EMULATOR=True

# WebSocket server URL - replace with your actual WebSocket endpoint
WEBSOCKET_URL=wss://your-websocket-server.com/socket

# JWT token for authentication - replace with your actual token
JWT_TOKEN=your-jwt-token-here

# Optional: Additional configuration variables
# Uncomment and modify as needed for your setup

# Matrix configuration (if needed)
# MATRIX_ROWS=32
# MATRIX_COLS=64
# MATRIX_CHAIN_LENGTH=1
# MATRIX_PARALLEL=1
# MATRIX_BRIGHTNESS=50

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO